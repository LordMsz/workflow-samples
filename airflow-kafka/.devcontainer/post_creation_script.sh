pip install apache-airflow

# initializes the database, creates a user, and starts all components in the background
airflow standalone & # TODO: for some reason this lets the devcontainer start up, but the airflow doesn't start